[project]
name = "prompt-evaluation-case-studies"
version = "0.1.0"
description = "Small Python examples for AI output evaluation rubrics and localization checks."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
